markdown: kramdown

defaults:
  - scope:
      path: "assets/img"
    values:
      image: true

# Assets
#
# We specify the directory for Jekyll so we can use @imports.
sass:
  sass_dir:          _scss
  style:            :compressed
